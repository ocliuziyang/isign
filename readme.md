# isign (未完成 / uncompleted)
Build on Laravel &amp; Vue (uncompleted)

## 安装

    $ git clone https://github.com/ocliuziyang/isign.git
    
    $ cd isign
    
    $ composer install
    
    $ cp .env.example .env
    
    $ npm/cnpm install
    
    $ bower install
    
    $ gulp copyFiles
    
    $ npm run watch
    
